---
'@neovici/cosmoz-datetime-input': major
---

Upgrade to typescript. Ships a dist build with an exports map (`.` and `./cosmoz-datetime-input`) and depends on `@neovici/cosmoz-tokens` and `@neovici/cosmoz-input` v6. Preserve undefined date/time values when editing only date or time.
