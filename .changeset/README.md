# Changesets

Hello and welcome! This folder has been generated by `@changesets/cli`.

A changeset is a way to group together changes and release them together. If you need to make a change that should be released, create a changeset:

```bash
pnpm changeset
```

For more information about changesets, [see the documentation](https://github.com/changesets/changesets).